# Аудит активации модулей: cvd, open_interest, oi_delta, funding_rate, fibonacci

**Метод:** инструментальный прогон 24 мес (17 019 баров 1h, окно 600) со счётчиками
на каждом условии целевых модулей + статический разбор кода. Скрипт:
`backend/scripts/module_audit.py` (read-only, стратегия не изменялась).
**Данные:** синтетическая серия (из контейнера нет доступа к Binance). Для
open_interest / oi_delta / funding_rate выводы **не зависят от данных** —
причина структурная. Для cvd / fibonacci воронки условий и методика порогов
переносимы, абсолютные цифры на реальных данных будут отличаться —
скрипт перезапускается на реальной истории той же командой.

---

## Сводка активаций за 24 месяца (17 019 баров)

| Модуль | Активаций | % баров | Отфильтровано |
|---|---|---|---|
| open_interest | **0** | 0.0% | 17 019 (100%) |
| oi_delta | **0** | 0.0% | 17 019 (100%) |
| funding_rate | **0** | 0.0% | 17 019 (100%) |
| cvd | 361 | 2.1% | 16 658 (97.9%) |
| fibonacci | 1 980 | 11.6% | 15 039 (88.4%) |
| _для сравнения: vwap_ | _17 019_ | _100%_ | — |
| _liquidity_sweep_ | _10 566_ | _62%_ | — |

---

## 1. open_interest — 0 активаций

1. **Активаций:** 0 из 17 019.
2. **Отфильтрован:** 17 019 раз (100%).
3. **Условие отбраковки:** первая же проверка `if oi is None or len(oi) < 6: return neutral("Нет данных Open Interest")`. В бэктесте `precompute.py:46` создаёт контекст как `MarketContext(symbol, timeframe, df, htf_df)` — поле `open_interest` всегда `None`.
4. **Пороги** (недостижимы из-за п.3): `|ΔOI(5 баров)| ≥ 0.5%`; сила сигнала `0.5 + ΔOI×20`.
5. **% отсева кандидатов:** модуль не отсеивает кандидатов напрямую, но его мёртвый вес блокирует семейство flow — см. раздел «Системный эффект».
6. **Ошибка в логике активации:** в самом модуле — нет; **архитектурная ошибка в подаче данных**: (а) бэктест не передаёт OI в контекст; (б) даже при желании — Binance `/futures/data/openInterestHist` отдаёт историю только за ~30 дней, 24-месячный OI-бэктест невозможен без собственного коллектора. В live сканер OI передаёт (`scanner.py:36`) — модуль работает только там. Дополнительно (из аудита v1): ряд OI не выравнивается с ценой по timestamp.
7. **Предложение:** порогами не лечится. (а) Поднять коллектор OI (писать `openInterestHist` в БД каждые 15 мин) — честный OI-бэктест станет возможен только вперёд с момента запуска; (б) для немедленной пользы — бэктесты ≤30 дней с реальным OI. Прогноз активаций: по live-порогу |ΔOI|≥0.5%/5 баров на крипте срабатывание ожидаемо на 10–25% баров (волатильность OI высока), но подтвердить можно только после коллектора.

## 2. oi_delta — 0 активаций

1–3. Идентично open_interest: тот же `ctx.open_interest is None`, отбраковка на `len(oi) < 12`.
4. **Пороги:** схлопывание `d_fast < −2%` при `|Δцены| > 0.4%`; приток `accel > 0.8%` и `d_fast > 1%`.
5. Прямого отсева нет; усиливает мёртвый вес семейства flow (вес 0.9).
6. Ошибки активации в модуле нет; причина — данные (см. п.1.6).
7. Порогами не лечится; решение то же — коллектор. Замечание к порогам на будущее: `d_fast < −2%` за 3 точки 15-минутного OI — редкое событие вне каскадов ликвидаций; для 1h-периода OI разумнее −1%.

## 3. funding_rate — 0 активаций (и упущенная возможность)

1. **Активаций:** 0 из 17 019.
2. **Отфильтрован:** 17 019 раз (100%).
3. **Условие:** `if f is None or f.empty: return neutral("Нет данных Funding Rate")` — бэктест funding в контекст не передаёт.
4. **Пороги** (недостижимы): |avg₃| ≥ 0.10% — сильный сигнал; ≥ 0.05% — умеренный; перцентильные экстремумы p90/p10 по истории 30 точек.
5. Прямого отсева нет; блокирует семейство sentiment в паре с liquidation (см. ниже).
6. **Найдена конкретная ошибка интеграции:** `backtester.py` **уже скачивает** историю funding (`client.funding_rate(symbol, limit=1000)`, строка 32), но использует её только для расчёта funding-платежей в издержках — в `MarketContext` модуля она не попадает. Данные есть — провод не подключён. Вторая проблема: `limit=1000` × 8ч = ~333 дня; для 24 мес нужна пагинация по `startTime`.
7. **Предложение (без изменения порогов):** пробросить уже скачанный ряд funding в `precompute_candidates` срезом «по время бара» (без заглядывания вперёд) + пагинация до 24 мес. Прогноз активаций: на реальном BTC funding выходит за ±0.05% в ~10–20% 8-часовых периодов (исторически), т.е. ожидаемо **~1 500–3 500 активных баров/24 мес** — модуль оживёт без правки порогов. Точную цифру даст перезапуск аудита после подключения данных.

## 4. cvd — 361 активация (2.1%), две из трёх веток мертвы

1. **Активаций:** 361 (flow-подтверждение — 360, абсорбция — **1**, дивергенция — **0**).
2. **Отфильтрован:** 16 658 раз.
3. **Воронка отбраковки:**

| Условие | Баров | % |
|---|---|---|
| `\|strength\| ≤ 0.08` (порог flow-ветки) | 11 534 | 67.8% |
| экстремум цены есть, но `gap ≤ 0.15·volₙ` (дивергенция) | 4 898 | 28.8% |
| знак strength ≠ знак Δцены | 226 | 1.3% |
| **активации** | **361** | **2.1%** |

4. **Пороги:** flow `|strength| > 0.08`; дивергенция `gap > 0.15·vol_norm`; абсорбция `|Δцены| < 0.2%` и `|strength| > 0.12`.
5. **% отсева:** сам модуль кандидатов не режет, но он — второй по весу в семействе flow (1.2); его молчание + мёртвые OI-модули → flow подтвердился лишь **193/17 019 (1.1%)** баров.
6. **Ошибка логики: пороги не соответствуют распределению величины.** Измерено: медиана `|strength|` = 0.027, **p90 = 0.065, p99 = 0.0998** — порог 0.08 лежит выше p90, т.е. ветка flow по построению ловит <5% баров. Хуже с дивергенцией: дефицит CVD на экстремумах имеет **p90 = 0.059**, порог 0.15 — за пределами p99.9: **0 срабатываний за 24 месяца** — мёртвый код. Абсорбция (0.12 при p99=0.0998) — 1 срабатывание за 2 года. Это классическая ошибка «порог из головы вместо порога из распределения».
7. **Предлагаемые пороги и прогноз (измерено на датасете):**

| Ветка | Текущий порог | Активаций | Предлагаемый | Прогноз активаций |
|---|---|---|---|---|
| flow | 0.08 | 361 | **0.03 (~p55)** | **3 962 (×11.0)** |
| flow (умеренный вариант) | 0.08 | 361 | 0.05 (~p80) | 1 918 (×5.3) |
| дивергенция | 0.15 | 0 | **0.05 (~p85)** | **796** |
| дивергенция (мягкий) | 0.15 | 0 | 0.03 | 1 899 |
| абсорбция | 0.12 | 1 | 0.06 (~p88) | оценочно 150–400* |

\* комбинированное условие с флэтом цены; точный замер — после выбора порога.
**Лучше констант:** адаптивный порог = скользящий перцентиль p80 собственного `|strength|` за 30 дней — самокалибровка под инструмент и режим (на реальных данных распределение шире синтетического, константы снова промахнутся).

## 5. fibonacci — 1 980 активаций (11.6%): активируется, но «не участвует»

1. **Активаций:** 1 980.
2. **Отфильтрован:** 15 039.
3. **Воронка:**

| Условие | Баров | % |
|---|---|---|
| структура в боковике (`trend == range`) | 3 930 | 23.1% |
| неверный порядок свингов (последние high/low не образуют ногу по тренду) | 6 829 | **40.1%** |
| тренд и порядок ок, но цена дальше 0.35·ATR от всех 4 уровней | 4 280 | 25.1% |
| **активации** | **1 980** | **11.6%** |

4. **Пороги:** толеранс 0.35·ATR; уровни 0.5/0.618/0.705/0.786; пара «последний swing high + последний swing low».
5. **% отсева кандидатов:** не режет; вклад в семейство liquidity мал (вес 1.0 из 6.2 семейства).
6. **Ошибка логики — да, в выборе ноги:** главный фильтр (40.1%!) — `wrong_swing_order`. Модуль берёт «последний high и последний low», но в развитом тренде последним часто оказывается свинг не той стороны (после impulse-leg формируется новый low выше — порядок «ломается»), и валидная нога отбрасывается. Корректно — искать последнюю *импульсную ногу*: для аптренда — последний значимый low **перед** последним высшим high, а не глобально последний low. Это вернуло бы большую часть из 6 829 баров в рассмотрение. Второй вывод: «не участвует в сделках» при 11.6% активаций — потому что attribution показал низкий вклад: модуль активируется *в случайных местах* (во флэте уровни от шумовых свингов), что подтверждает вердикт «statistically_useless» из прошлого анализа. Проблема не «слишком редко», а «слишком неразборчиво».
7. **Прогноз по толерансу** (распределение расстояний: p50 = 0.88·ATR, p25 = 0.21):

| Толеранс | Активаций |
|---|---|
| 0.35·ATR (тек.) | 1 980 |
| 0.5·ATR | 2 322 (+17%) |
| 0.75·ATR | 2 875 (+45%) |
| 1.0·ATR | 3 341 (+69%) |

Рекомендация противоположна расширению: толеранс **не трогать** (или сузить до 0.3 с зоной OTE 0.62–0.79 как единым диапазоном), а чинить выбор ноги (п.6) и добавить требование импульсности ноги (≥N·ATR за ≤M баров) — это поднимет качество, а не количество.

---

## Системный эффект: почему мёртвые модули блокируют сделки

Семейство подтверждается при взвешенном балле `F > 0.15`, где знаменатель —
**суммарный вес всех модулей семейства, включая мёртвые**.

**Flow** = volume(1.0) + cvd(1.2) + oi(1.0) + oi_delta(0.9) + vwap(1.0), Σw = 5.1.
При мёртвых OI-модулях и редком cvd максимум одиночного vwap:
`1.0·0.75·1.0 / 5.1 = 0.147 < 0.15` — **семейство flow математически не может
подтвердиться одним vwap ни при каких условиях**. Факт: flow подтвердился
193/17 019 (1.1%) — только при совпадении volume+cvd.

**Sentiment** = funding(0.8, мёртв) + liquidation(0.9), Σw = 1.7: подтверждается
только сильными срабатываниями liquidation (1 286 раз, 7.6%).

**Цена вопроса:** 4 903 кандидата (28.8%) остановились ровно на 2 семействах,
из них **4 130 (84%, или 24.3% всех баров)** не имели ни flow, ни sentiment —
основной канал недобора третьего подтверждения. Подключение funding (готовые
данные!) и калибровка cvd-порогов напрямую увеличат проходимость min_families=3.

Сопутствующая логическая ошибка (фиксирую, не правлю): знаменатель семейства
включает вес модулей, вернувших «нет данных» — отсутствие данных штрафуется
как нейтральное мнение. Корректнее исключать no-data модули из Σw семейства.

---

## Приоритеты исправлений (по убыванию эффекта, для отдельного спринта)

1. Пробросить **уже скачиваемый** funding в контекст бэктеста + пагинация до 24 мес (нулевая стоимость данных, оживляет sentiment).
2. Исключать no-data модули из знаменателя семейства (разблокирует flow для vwap+volume).
3. Перекалибровать пороги cvd по перцентилям (дивергенция 0.15 → ~0.05; flow 0.08 → 0.03–0.05) или перейти на адаптивные.
4. fibonacci: выбор последней импульсной ноги вместо «последних свингов» + критерий импульсности.
5. Коллектор OI для будущих периодов; до него — OI-модули честно живут только в live.
